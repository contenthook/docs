# Flags

In this sub-section of the cli section, we will cover all the flags that are available to the CLI and its commands.

## Flags
- `--help` - Display help for the command, Alias: `-h`
- `--version` - Display version information, Alias: `-v`

## Flags for `contenthook init`
- `--key` - The API key for the project