# Run

The `run <command>` command is used to run a command specified in the contenthook config.

## Usage

```bash copy
$ contenthook run <command>
$ contenthook run --help
# or
$ contenthook run -h
```