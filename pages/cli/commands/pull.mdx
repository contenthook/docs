# Pull

The `pull` command is used to pull the content from the ContentHook Cloud to your local project.

## Usage

```bash copy
$ contenthook pull
$ contenthook pull --help
# or
$ contenthook pull -h
```