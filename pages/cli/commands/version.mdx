# Version

The `version` command is used to display the version of the `contenthook` CLI tool.

## Usage

```bash copy
$ contenthook version
$ contenthook version --help
# or
$ contenthook version -h
```